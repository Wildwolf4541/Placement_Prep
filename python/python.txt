string, tuples, integers, boolean, floats are immutable
Only list, set, dictionary, array, bytearray are mutable.

Everything is an object in python.

python variable ka datatype ni hota, memory ko assign hota hai datatype.
slicing makes copy of the reference.

Iteratable Objects-> lists, dict, file
Iteration tools-> for, comprehension
__next__

lambda helps store small functions in a variable_name.

*args       → multiple positional arguments → tuple
**kwargs    → multiple keyword arguments   → dictionary

yield-> used in a function to return a generator object, 
        which can be iterated over using a for loop 
        or converted into a list using the list() function.

scopes and closures
